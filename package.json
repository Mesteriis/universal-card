{
  "name": "universal-card",
  "version": "1.0.0",
  "description": "Advanced Lovelace card for Home Assistant",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "dev": "node build.js --dev",
    "smoke:themes-modes": "node scripts/smoke-theme-mode-matrix.js",
    "test": "npm run build && npm run smoke:themes-modes",
    "check": "npm run build && npm run smoke:themes-modes"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Mesteriis/universal-card.git"
  },
  "author": "Mesteriis",
  "license": "MIT"
}
